#include<stdio.h>
int main()
{
intnum, fact=1;
int *pn, *pf;
printf("Enter any number: ");
scanf("%d", &num);
pn=&num;
pf=&fact;
do
{
*pf=(*pf)*(*pn);
*pn - *pn -1;
}
while(*pn>0);
printf("\n Factoria of ented number is : %d",*pf);
return(0);
}
